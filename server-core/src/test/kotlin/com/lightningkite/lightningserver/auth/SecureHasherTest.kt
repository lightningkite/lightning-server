package com.lightningkite.lightningserver.auth

import org.junit.Test

class SecureHasherTest {
    @Test
    fun test() {
//        val hasher = SecureHasher.ECDSA256("")
//        hasher.sign(byteArrayOf(1, 2, 3, 4, 5, 6))
    }
}